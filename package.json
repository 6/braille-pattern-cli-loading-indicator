{
  "name": "braille-pattern-cli-loading-indicator",
  "version": "0.0.0",
  "description": "CLI loading indicator using braille patterns",
  "main": "index.js",
  "scripts": {
    "test": "standard",
    "preversion": "npm test",
    "postversion": "npm publish && git push && git push --tags"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "chalk": "^1.1.3",
    "standard": "^7.1.0"
  },
  "dependencies": {
    "ansi": "^0.3.1"
  }
}
